import "stdio.enc";

// FuntionCalls //

call print with "wow", "this", "is", "neat".
call print.

// Variables //

set constant x to 5 + 5. // Constant
set y to 5.

// Functions //

create function cool with test. {
  call print with test.
}

call cool with x.
call print with y.

// If statements //

create function factorial with num. {
  {
    return 1.
  } if num is equal to 1 else {
    return num * call factorial with num - 1.
  }
}

create function testFunc with anotherFunc. {
  return call anotherFunc with 3.
}

call print with call testFunc with factorial.

call print with call factorial with 5. // Factorial 5 is 120 so yea